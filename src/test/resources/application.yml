spring:
  redis:
    delay-queue:
      enabled: true
      broker-scheduler-period: 500
      message-validity-time: 30
      producer-queue-num: 1
    host: 127.0.0.1
    lock:
      type: redisson
  ratelimiter:
    enabled: true
#    redis-address: redis://127.0.0.1:6379
#    redis-database: 1
    status-code: 555
    response-body: {"code":555,"msg":"Config Response Too Many Requests"}
#  caffeine-cache:
#    enabled: true
#    expiration-duration: 20
#    initial-capacity: 200
#    maximum-size: 1000
#    mode: expire_after_write
#    time-unit: seconds
logging:
  level:
    com.github.qyl: debug
